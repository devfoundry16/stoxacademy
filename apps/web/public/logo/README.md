# Logo files

Place your locale-specific logos here:

- **logo-en.png** – shown when the site is in English
- **logo-ar.png** – shown when the site is in Arabic

Supported formats: PNG, JPG, WebP, or SVG. If you use a different format or filename, update the `logoSrc` logic in `src/components/header.js`.
